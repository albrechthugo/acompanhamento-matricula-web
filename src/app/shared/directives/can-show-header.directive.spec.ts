import { CanShowHeaderDirective } from './can-show-header.directive';

describe('CanShowHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CanShowHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
