import { DocumentTypePipePipe } from './document-type-pipe.pipe';

describe('DocumentTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
