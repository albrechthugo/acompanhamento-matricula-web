import { GradeStatusPipe } from './grade-status.pipe';

describe('GradeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
