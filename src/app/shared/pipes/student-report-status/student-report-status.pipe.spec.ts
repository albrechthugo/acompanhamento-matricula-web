import { StudentReportStatusPipe } from './student-report-status.pipe';

describe('StudentReportStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentReportStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
